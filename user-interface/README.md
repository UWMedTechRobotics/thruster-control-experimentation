# User Interface

## Setup
1. Start venv using the following:
```bash
python -m venv venv
source ./venv/bin/activate
```

Note that typing 'deactivate' into terminal will end your virtual environment session.

